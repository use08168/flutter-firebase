import 'package:flutter/material.dart';

class MainDiscoverScreen extends StatelessWidget {
  const MainDiscoverScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: const Text("main Discover"),
      ),
    );
  }
}
